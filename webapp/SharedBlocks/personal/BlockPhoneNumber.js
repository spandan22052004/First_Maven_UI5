sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPhoneNumber = BlockBase.extend("sample.project1.SharedBlocks.personal.BlockPhoneNumber", {
			metadata: {}
		});


		return BlockPhoneNumber;

	});
